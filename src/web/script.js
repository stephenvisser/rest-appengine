  var Todo = Backbone.Model.extend({
	  
  });

  // The Application
  // ---------------



// This is JQuery for: 'When the DOM is ready, perform this function'
$(function(){
	// Our overall **AppView** is the top-level piece of UI.
	var AppView = Backbone.View.extend({
		  
	
	});

	
	new DocumentRow({
		  model: doc,
		  id: "document-row-" + doc.id
		});
});
